import { FC } from "react";
import "./Header.scss";

const Header: FC = () => {
  return (
    <section id="Header">
      <div className="container">
        <div className="Header">Header</div>
      </div>
    </section>
  );
};

export default Header;
