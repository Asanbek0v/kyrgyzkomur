import { FC } from "react";
import "./Header.scss";

const Header: FC = () => {
  return (
    <section id="Header">
      <div className="container">
        <div className="Header"></div>
      </div>
    </section>
  );
};

export default Header;
